<%= render "layouts/banner" %>


    <div class="col-sm-8 col-sm-offset-2">
      <div class="search-row">
        <%= form_tag billboard_path, method: :get do %>
        <!-- <div class="col-xs-10 col-sm-8 col-md-3 col-lg-2"> -->
          <div class="form-group col-xs-10 col-sm-8 col-md-4 col-lg-3">
            <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Cuisine" %>
          </div>
          <div class="form-group col-xs-10 col-sm-8 col-md-4 col-lg-3">
            <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Neighborhood" %>
          </div>
          <div class="form-group col-xs-10 col-sm-8 col-md-4 col-lg-3">
            <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Price Range" %>
          </div>
        <!-- </div> -->
        <div class="col-xs-10 col-sm-8 col-md-2 col-lg-2">
          <%= submit_tag 'Search', name: nil, class: "btn-search" %>
        </div>
        <% end %>
      </div>
    </div>

